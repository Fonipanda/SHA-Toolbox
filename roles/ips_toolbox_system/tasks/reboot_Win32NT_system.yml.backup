---

- name: Windows Reboot
  win_reboot:
  register: rebootresult

- name: set_results
  include_role:
    name: ips_toolbox_set_results
  vars:
    set_results_state:
      "{% if (rebootresult.rebooted == true) %}\

         rebooted\

      {% else %}\

         not rebooted\

      {% endif %}"
    set_results_item: ""
    set_results_value: ""
    set_results_component: "{{ system_component }}"
    set_results_operation: "{{ system_operation }}"
    set_results_role_name: "{{ system_role_name }}"
    set_results_result_name: "{{ system_result_name }}"
    set_results_status: "OK"

