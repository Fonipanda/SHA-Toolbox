---

- name: "Initialisation du rôle banner"
  ansible.builtin.set_fact:
    banner_component: "banner"
    banner_operation: "{{ banner_operation | default('create') }}"

- name: "Création de la bannière MOTD"
  ansible.builtin.template:
    src: motd.j2
    dest: /etc/motd
    mode: '0644'
    owner: root
    group: root
  ignore_errors: true

- name: "Création de la bannière issue (console locale)"
  ansible.builtin.template:
    src: issue.j2
    dest: /etc/issue
    mode: '0644'
    owner: root
    group: root
  ignore_errors: true
  when: ansible_os_family != "Windows"

- name: "Création de la bannière issue.net (SSH)"
  ansible.builtin.template:
    src: issue.j2
    dest: /etc/issue.net
    mode: '0644'
    owner: root
    group: root
  ignore_errors: true
  when: ansible_os_family != "Windows"

- name: "Configuration SSH pour afficher la bannière"
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: '^#?Banner'
    line: 'Banner /etc/issue.net'
    backup: yes
  notify: restart sshd
  ignore_errors: true
  when: ansible_os_family != "Windows"

- name: "Affichage du statut de création des bannières"
  ansible.builtin.debug:
    msg:
      - "✅ Bannières créées pour {{ ansible_hostname }}"
      - "Application: {{ banner_codeAP | default('N/A') }}"
      - "Middlewares détectés: {{ banner_middlewares | default('Aucun') }}"
